= simple_form_for @studentsubject, defaults: { wrapper_html: { class: 'form-group' }, input_html: { class: 'form-control' } }, html: { multipart: true } do |f|

  = f.association :student
  = f.association :subject
  = f.submit  